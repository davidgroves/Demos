#!/usr/bin/env

a = "string"    # a string
b = 3           # an integer

# We can't add these together. We expect an error and will get one.

c = a + b

print("We blow up before here, so this never runs !")